{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && python manage.py collectstatic --noinput --no-input && python manage.py migrate --no-input"
  },
  "deploy": {
    "startCommand": "gunicorn bfg.wsgi --bind 0.0.0.0:$PORT --log-file -",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
